//#region src/index.ts
/** Host loader entry for the browser-only skin plugin (EAC-compatible). */
/** Provides no host-side behavior; EAC does not expose themeCatalog. */
function apply() {}
//#endregion
export { apply };
